name: Test Matrix

on:
  workflow_dispatch:
  pull_request:

jobs:
  matrix:
    strategy:
      matrix:
        python: ["3.12"]
    uses: Architit/RADRILONIUMA-PROJECT/.github/workflows/submodule-gate.yml@submodule-gate-v1.0.0
    with:
      python_version: ${{ matrix.python }}
      submodule_paths: |
        LAM/default/agents/comm-agent
        LAM/default/agents/codex-agent
        LAM/default/agents/roaudter-agent
        LAM/default/agents/operator-agent
        LAM/default/agents/test-agent
      install_command: ./devkit/bootstrap.sh
      test_command: ./devkit/check.sh --profile ci
    secrets: inherit
